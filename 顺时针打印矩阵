# -*- coding:utf-8 -*-
# 方法一  循环输出
# 方法二 去掉第一行  逆时针旋转
class Solution:
    # matrix类型为二维列表，需要返回列表
    def printMatrix(self, matrix):
        # write code here
        res = []
        while matrix:
            res += matrix.pop(0)
            matrix = list(zip(*matrix))[::-1]
        return res

#coding:utf-8
class ListNode:
    def __init__(self, x):
        self.val = x
        self.next = None
#
# 代码中的类名、方法名、参数名已经指定，请勿修改，直接返回方法规定的值即可
#
# 
# @param pHead1 ListNode类 
# @param pHead2 ListNode类 
# @return ListNode类
#
# 方法一
class Solution:
    def Merge(self , pHead1 , pHead2 ):
        # write code here
        head1 = pHead1
        head2 = pHead2
        newhead = ListNode(0)
        node = newhead
        while head1 and head2:
            if head1.val < head2.val:
                node.next = head1
                node = node.next
                head1 = head1.next
            else:
                node.next = head2
                node = node.next
                head2 = head2.next
        if head1:
            node.next = head1
        if head2:
            node.next = head2
        return newhead.next

# 方法二
# 递归深度太深了 报错了
# 而且新旧节点混用
# class Solution:
#     def Merge(self , pHead1 , pHead2 ):
#         def mergenode(node1, node2):
#             if not node1:
#                 return node2
#             if not node2:
#                 return node1
#             if node1.val < node2.val:
#                 newnode = ListNode(node1.val)
#                 newnode.next = mergenode(node1.next, node2)
#             else:
#                 newnode = ListNode(node2.val)
#                 newnode.next = mergenode(node1, node2.next)
#             return newnode
#         newhead = ListNode(0)
#         newhead.next = mergenode(pHead1, pHead2)
#         return newhead.next
# 递归深度太深了 报错了
# class Solution:
#     def Merge(self , pHead1 , pHead2 ):
#         if not pHead1:
#             return pHead2
#         if not pHead2:
#             return pHead1
#         if pHead1.val < pHead2.val:
#             pHead1.next = self.Merge(pHead1.next, pHead2)
#             return pHead1
#         else:
#             pHead2.next = self.Merge(pHead1, pHead2.next)
#             return pHead2

            
